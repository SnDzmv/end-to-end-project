# Implementation plan — Неделя 1

1. **Установить Miniconda** — подготовить Python-среду.  
2. **Создать репозиторий** — публичный GitHub с базовой структурой: `/src`, `/data`, `/notebooks`, `/docs`, `/configs`, `/tests`, `/scripts`.  
3. **Добавить основные файлы** — `broken_env.py`, `requirements.txt`, `README.md`, документацию в `/docs`.  
4. **Разработать setup_env.bat** — проверка conda, создание окружения, установка зависимостей, запуск теста `broken_env.py`.  
5. **Протестировать процесс** — запуск `.bat`, убедиться, что выводится `[OK]`.  
6. **Подготовить сдачу** — ZIP репозитория для загрузки на платформу.


# Неделя 2

## Цель
Реализовать Extract данных из World Bank API для варианта 13 (ЮАР, безработица %).

---

## Этапы реализации
1. Подготовка окружения:
   - Установить Python-библиотеки: `requests`, `pyyaml`.
2. Подключение к API:
   - Использовать URL и параметры из `configs/variant_13.yml`.
3. Отправка запроса и получение данных:
   - Сделать GET-запрос с таймаутом.
   - Обработать возможные HTTP ошибки.
4. Сохранение сырых данных:
   - Сохранить JSON в папку `data/raw/` с timestamp в имени файла.
5. Тестирование:
   - Проверить, что JSON корректно сохраняется.
   - Убедиться, что структура проекта соответствует требованиям.

---

## Выходные артефакты
- Скрипт: `src/extract.py`
- Конфигурация: `configs/variant_13.yml`
- Raw JSON: `data/raw/raw_YYYYMMDD_HHMMSS.json`
