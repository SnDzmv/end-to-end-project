# LLM Usage Log — Неделя 1

**Дата:** 2026-02-16 – 2026-03-01  
**Цель:** Подготовка среды и каркаса проекта  
**Используемая модель:** GPT-5-mini 

## Использование LLM

1. **Понимание задания** — объяснил, что такое conda, окружения, `.bat`.  
2. **Разбор setup_env.bat** — пошагово объяснил команды и их назначение.  
3. **Работа с путями** — разъяснил, как работать с OneDrive и пробелами в путях.  
4. **Проверка и установка зависимостей** — как ставить pandas внутри окружения.  

## Комментарии

- Все действия делались с минимальным ручным вводом команд.  
- LLM помог понять различия между обычным Python и conda, и как сделать проект воспроизводимым на Windows.

# Неделя 2

## Цель
Использование LLM для помощи в разработке Extract скрипта и подготовки документации.

---

## Задачи, решенные с помощью LLM
1. Объяснение работы YAML-конфигурации и структуры API.
2. Написание рабочего примера `extract.py` для получения raw JSON.
3. Объяснение разницы между относительными и абсолютными путями в Python.
4. Подготовка краткого и развернутого README.md для недели 2.
5. Консультации по архитектуре скрипта, обработке ошибок и сохранению файлов.
6. Создание шаблонов `Implementation_Plan.md` и `LLM_Usage_Log.md`.

---

## Комментарии
- LLM помог структурировать код и документацию так, чтобы преподаватель мог сразу проверить выполнение.
- Были даны рекомендации по корректной работе с относительными путями и сохранением данных.


# Неделя 3  
**Цель**  
Использование LLM для помощи в анализе и нормализации данных из raw JSON и подготовки документации.

**Задачи, решенные с помощью LLM**  
- Объяснение структуры JSON и вложенных списков/словарей.  
- Подсказки по выбору зерна таблицы для нормализованных данных.  
- Рекомендации по созданию DataFrame с помощью `pd.json_normalize`.  
- Помощь в приведении типов данных (`date` → datetime, `value` → numeric).  
- Консультации по первичной очистке: переименование колонок, удаление лишних полей, работа с пропусками и дубликатами.  
- Подготовка шаблонов README.md, Data_Contract.md и Implementation_Plan.md для недели 3.  

**Комментарии**  
LLM помог понять, как структурировать данные и как работать с Pandas, чтобы получить нормализованный CSV из raw JSON.  
Были даны советы по проверке типов данных и пропусков, а также рекомендации по организации ноутбука и сохранению файлов в нужную директорию.  
Все предложенные шаги проверены и реализованы вручную. 


# Неделя 4


LLM использовался как инструмент поддержки разработки.

---

## Задачи, решенные с помощью LLM

- объяснение концепции data mart
- объяснение join и кардинальности
- разбор проблемы many-to-many
- генерация примеров merge в Pandas
- помощь в расчете KPI
- помощь в работе с временными рядами
- генерация структуры проекта
- помощь в написании документации

---

## Что было сделано самостоятельно

- понимание структуры данных
- выбор метрик
- проверка корректности join
- запуск и тестирование кода

---

## Вывод

LLM использовался как вспомогательный инструмент, но ключевые решения по структуре данных и метрикам принимались самостоятельно.

# Неделя 5

## Что сделано самостоятельно

- Подготовка среды: установка Docker и Postgres  
- Проверка и понимание структуры данных: колонки, KPI, диапазон годов  
- Проверка работы Postgres через Docker
- Запуск и тестирование скриптов на Python на локальной машине  

## Что сделано с помощью LLM

- Подсказки по правильному синтаксису SQLAlchemy для Python 3.13  
- Примеры идемпотентной загрузки данных в Postgres (`load.py`)  
- Создание скрипта `sql_checks.py` для выполнения 5 обязательных SQL-проверок  
- Рекомендации по проверкам: дубли, NULL, агрегированные метрики  

## Итог

Использование LLM позволило ускорить написание скриптов, но основная работа по подготовке данных, поднятию базы и запуску проверок была выполнена самостоятельно

# Неделя 6

LLM использовался как вспомогательный инструмент для разработки ETL-пайплайна.

---

## Что было сделано с помощью LLM

- Проектирование архитектуры pipeline (full / incremental)
- Объяснение концепций:
  - watermark
  - business key
  - state management
  - idempotency
- Помощь в исправлении pipeline.py
- Объяснение ошибок PostgreSQL подключения
- Улучшение структуры ETL-слоёв

---


LLM использовался как инструмент поддержки разработки и объяснения архитектуры, но:
- код проекта был интегрирован и протестирован вручную
- структура ETL была адаптирована под требования задания
- финальная отладка выполнялась самостоятельно

# Неделя 7

## Проектирование структуры ноутбука
- помощь в разбиении ноутбука на логические блоки:
  - загрузка данных
  - обработка времени
  - построение графиков
  - формулировка выводов
- подсказки по тому, как правильно организовать анализ (вопрос → график → вывод)

## Что было сделано самостоятельно

- реализация ноутбука `week7_viz.ipynb`
- загрузка и обработка mart-данных
- построение всех графиков в matplotlib
- работа с датами и сортировка временного ряда
- формулировка финальных выводов по результатам анализа
- проверка воспроизводимости ноутбука

---

## Итог

LLM использовался как инструмент поддержки:

- для объяснения принципов визуализации данных  
- для помощи в проектировании структуры анализа  
- для улучшения качества графиков и интерпретации 


# Неделя 8

##  Общая информация

LLM использовался как вспомогательный инструмент для:
- понимания концепций Data Quality
- проектирования структуры проверок
- генерации шаблонов кода

---

##  Что было сделано с помощью LLM

- объяснение принципов Data Quality (PASS / FAIL / WARNING)
- помощь в формулировании DQ-правил
- генерация структуры файла `dq.py`
- помощь в написании unit-тестов
- объяснение ошибок (например, проблемы с импортами)
- помощь в оформлении документации (README, dq.md)

---

##  Что было сделано самостоятельно

- выбор конкретных проверок под проект
- адаптация кода под структуру проекта
- настройка путей и запусков
- тестирование на реальных данных
- внесение искусственных ошибок для проверки
- интеграция DQ в pipeline

---

##  Итог

LLM использовался как инструмент ускорения разработки и обучения,  
но основная логика, адаптация и тестирование выполнены самостоятельно.


# Неделя 10

# Цель использования

LLM использовался как вспомогательный инструмент

- объяснение Docker и Docker Compose
- настройка PostgreSQL и Metabase
- помощь с docker-compose.yml
- диагностика подключения к PostgreSQL
- помощь с настройкой Metabase

---

# Какие задачи решались с помощью LLM

## Docker и контейнеризация

- объяснение concepts:
  - container
  - image
  - volume
  - port mapping
  - docker compose

- настройка docker-compose.yml

---

## PostgreSQL

- настройка подключения
- диагностика конфликта портов
- проверка загрузки данных через load.py

---

## Metabase

- подключение к PostgreSQL

---


# Итог

LLM использовался как инструмент консультирования и ускорения разработки, при этом:

- все контейнеры запускались локально;
- настройка Docker и Metabase выполнялась вручную;
- загрузка данных и создание dashboard выполнялись самостоятельно.


# Неделя 11

## Использование LLM

В процессе выполнения задания использовался LLM (ChatGPT) для:

---

## 1. Настройка Docker и Airflow

- помощь в создании docker-compose.yml
- исправление ошибок запуска Airflow
- диагностика проблем с Postgres и SQLite

---

## 2. Debugging ETL pipeline

- исправление ошибок подключения PostgreSQL
- диагностика ошибки "role does not exist"
- исправление конфигурации подключения внутри Docker network

---

## 3. Разработка DAG

- создание структуры DAG:
  extract → transform → load → dq
- настройка BashOperator задач
- исправление путей внутри контейнера

---

## 4. Data pipeline debugging

- исправление load.py (PostgreSQL host = postgres)
- исправление порта (5432 вместо 5433 внутри Docker)
- устранение ошибки подключения к базе

---

## Итог

LLM использовался как ассистент для:

- обучения Docker + Airflow
- исправления ошибок конфигурации
- построения ETL pipeline
